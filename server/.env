# Application Port
PORT=8000

# Database Configuration for Docker Compose
DB_HOST=127.0.0.1
DB_NAME=todotasks
DB_USER=root
DB_PASS= # You must set this password to match the one in docker-compose.yml!